#include "hw1.h"

int main(int argc, char** argv){
        if(argc != 2){
                printf("No or wrong arguments givven. Abort!\n");
                return -1;
        }
        else{
                int n = atoi(argv[1]);
        }
}